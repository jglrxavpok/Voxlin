package org.jglr.voxlin.utils

import org.joml.Vector2f
import org.joml.Vector3f

val X_AXIS = Vector3f(1f, 0f, 0f).toImmutable()
val Y_AXIS = Vector3f(0f, 1f, 0f).toImmutable()
val Z_AXIS = Vector3f(0f, 0f, 1f).toImmutable()

val X_AXIS2D = Vector2f(1f, 0f).toImmutable()
val Y_AXIS2D = Vector2f(0f, 1f).toImmutable()
